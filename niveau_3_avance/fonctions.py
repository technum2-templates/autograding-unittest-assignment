"""
Niveau 3 - Tests Unitaires Avancés
Fonctions avec logique complexe, collections, et cas limites
"""


def trier_et_filtrer(nombres, seuil):
    """
    Trie une liste de nombres et filtre ceux >= seuil.
    Retourne une liste vide si l'entrée est None.
    Lève une TypeError si seuil n'est pas un nombre.
    """
    # À implémenter par l'étudiant
    pass


def fusionner_dictionnaires(dict1, dict2):
    """
    Fusionne deux dictionnaires.
    Si une clé existe dans les deux, la valeur de dict2 prend le dessus.
    Retourne un dictionnaire vide si les deux sont None.
    Lève une TypeError si l'un des arguments n'est pas un dictionnaire.
    """
    # À implémenter par l'étudiant
    pass


def compter_occurrences(texte, caractere):
    """
    Compte le nombre d'occurrences d'un caractère dans un texte.
    La recherche est sensible à la casse.
    Retourne 0 si le texte ou le caractère est None.
    """
    # À implémenter par l'étudiant
    pass


def extraire_nombres(texte):
    """
    Extrait tous les nombres entiers d'une chaîne de caractères.
    Retourne une liste de nombres.
    Retourne une liste vide si le texte est None ou vide.
    """
    # À implémenter par l'étudiant
    pass


def valider_mot_de_passe(mdp):
    """
    Valide un mot de passe selon ces critères:
    - Au moins 8 caractères
    - Au moins une majuscule
    - Au moins une minuscule
    - Au moins un chiffre
    Retourne True si valide, False sinon.
    Lève une TypeError si mdp n'est pas une chaîne.
    """
    # À implémenter par l'étudiant
    pass


def calculer_statistiques(nombres):
    """
    Calcule min, max, moyenne et médiane d'une liste de nombres.
    Retourne un dictionnaire {'min': ..., 'max': ..., 'moyenne': ..., 'mediane': ...}.
    Lève une ValueError si la liste est vide.
    Retourne None si l'entrée est None.
    """
    # À implémenter par l'étudiant
    pass
